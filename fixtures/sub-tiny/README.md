# sub-tiny

A deliberately tiny BIDS dataset used by the handbook's examples and tests.

The files are empty placeholders — they contain no real imaging data, so they're safe to commit. The walker / manifest / QC examples only care about the *filenames and layout*, not the bytes.

If you want to extend this fixture with real (small) data, drop replacements in place. The BIDS layout will keep working.
